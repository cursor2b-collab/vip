# 生产环境变量（提交到 Git，Netlify/Vercel 构建时自动读取）
# 注意：不要在此文件中放置密钥或敏感信息

# 后端地址（Netlify/Vercel 的 redirects/rewrites 规则已将 /api/* 转发到此地址）
VITE_BACKEND_URL=https://admin.amjsvip.cc

# 始终使用 PHP 后端（phpGameClient，含 AES 解密拦截器）
# 不得设为 false，否则 Netlify 构建出的前端无法解密后端加密响应
VITE_USE_PHP_GAME_BACKEND=true

# 不使用 Supabase 认证，使用后端 /api/v1/auth/login
VITE_USE_SUPABASE_AUTH=false

# Supabase（留空，不走 Supabase 数据源）
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
