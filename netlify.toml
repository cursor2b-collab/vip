# Netlify 构建与发布配置（用户端 vip）

[build]
  command   = "npm run build"
  publish   = "dist"
  # 若仓库根目录不是 vip，可改为子目录，例如：
  # base     = "vip"

# SPA：所有路径回落到 index.html
[[redirects]]
  from    = "/*"
  to      = "/index.html"
  status  = 200
