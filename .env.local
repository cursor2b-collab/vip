# 开发环境：Vite proxy 把 /api/v1 代理到 VITE_BACKEND_URL，无 CORS 问题
# 生产环境（Vercel）：vercel.json rewrites 把 /api/v1/* 代理到 admin.amjsvip.cc

# houduan 后端地址（开发 Vite proxy 用）
VITE_BACKEND_URL=https://admin.amjsvip.cc

# 始终使用 houduan PHP 后端（/api/v1/game/*）
VITE_USE_PHP_GAME_BACKEND=true

# 禁用 ley 游戏接口和 bet-proxy（已统一使用 houduan）
# VITE_GAME_API_URL=   ← 不要设置，否则走 /ley/* 路径导致 CORS
# VITE_BET_PROXY_URL=  ← 不要设置

# Supabase（用于 Supabase 认证时）
VITE_SUPABASE_URL=https://pkapmnmmpaudroleorjw.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_G9jcln4rcX12un4f-F4g1w_9IagXnhw

# 不使用 Supabase 认证，使用后端 auth/login
VITE_USE_SUPABASE_AUTH=false
