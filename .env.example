# =========================================================
# 前端环境变量说明（复制此文件为 .env.local 后填写）
# =========================================================

# houduan 后端地址（开发时 Vite 把 /api 代理到此地址，VIP 等接口统一走 houduan）
# 本地开发：先启动 houduan（如 php start.php start），再设为本机地址，例如：
#   VITE_BACKEND_URL=http://localhost:8788
# 生产或远程后端：
VITE_BACKEND_URL=https://admin.amjsvip.cc

# 始终使用 houduan PHP 后端（true 即可，不要改为 false）
VITE_USE_PHP_GAME_BACKEND=true

# ⚠️ 不要设置以下变量，否则会走旧路径导致 CORS 错误：
# VITE_GAME_API_URL=    ← 禁止设置，会触发 /ley/* 请求
# VITE_BET_PROXY_URL=   ← 禁止设置，已弃用

# Supabase（可选，Supabase 认证时使用）
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# 是否使用 Supabase 认证（false = 使用后端 auth/login）
VITE_USE_SUPABASE_AUTH=false
