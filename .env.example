# ========== 与 ht 管理后台统一：Supabase（同一项目） ==========
# 项目 pkapmnmmpaudroleorjw，与 ht 使用同一套
VITE_SUPABASE_URL=https://pkapmnmmpaudroleorjw.supabase.co
# Publishable Key（Supabase Dashboard → Settings → API → Publishable key）
# 或使用 Anon Key (JWT) 也可
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_G9jcln4rcX12un4f-F4g1w_9IagXnhw

# 为 true 时用户端用 Supabase Auth 登录（与管理后台同一套账号）
VITE_USE_SUPABASE_AUTH=false

# 为 true 时轮播图/公告/游戏列表从 Supabase 读取（与 ht 同表）；未设时与 VITE_USE_SUPABASE_AUTH 同效
# VITE_USE_SUPABASE_DATA=true

# ========== 游戏接口：与 ht 统一走同一代理（宝塔 api.amjsvip.cc） ==========
VITE_BET_PROXY_URL=https://api.amjsvip.cc

# 新 JS 游戏接口根地址（配置后用户端游戏请求走 https://api.amjsvip.cc/ley/*，优先于 PHP /api/v1/game）
VITE_GAME_API_URL=https://api.amjsvip.cc

# 游戏退出后跳转地址（returnUrl）：接口商通常要求已备案/白名单域名，不要用 localhost
# 不设则用当前页面的 origin（开发时 localhost 可能被拒）
VITE_SITE_URL=https://你的正式域名.com

# ========== 业务 API（可选：与 ht 同后端时填 ht 的后端地址） ==========
# 生产环境 /api 由 Vercel 或 Nginx 代理；开发环境 Vite proxy 会用到
# VITE_API_URL=
# 美盛游戏接口后端地址，开发时 /api 代理到此（默认 https://admin.amjsvip.cc）
# VITE_BACKEND_URL=https://admin.amjsvip.cc

# ========== 美盛游戏接口（PHP 后端，默认启用） ==========
# 游戏链接走 /api/v1/game/enter，需后端配置 LEY_API_URL / LEY_ACCOUNT / LEY_API_KEY
# 设为 false 时才会走旧 bet-proxy（已弃用）
# VITE_USE_PHP_GAME_BACKEND=true

# 若前端用 Supabase 登录且出现 [10002] Token格式无效，需在「后端」.env 配置：
# SUPABASE_JWT_SECRET=<Supabase 项目 Settings → API → JWT Secret>
# 并保证 caipiao_member 表有对应用户（email/username 或 supabase_uid 与 Supabase 一致）

# 旧：新游戏接口开关，迁移到 bet-api 后可移除
# VITE_USE_NEW_GAME_API=true

# ========== 人民币充值：朝风支付（LTZF） ==========
# 商户号（朝风商户后台）
VITE_LTZF_MCH_ID=20000024
# 对接秘钥（朝风商户后台-安全中心，勿提交到仓库）
VITE_LTZF_KEY=BALNALJSF9ERMJCVICHQCITV4QX8CBABUGWJPDYSQVG1QJD88EQNZA9N0DUVX7D9QSPRU8QXEDMI2N9SOIIWWOFG82C9TLU27SH5FWRMFEOLNLTSTOOCFZPZDPTR2H58
# 统一下单接口地址，不填默认 https://cfapi.ltzf.xyz
# VITE_LTZF_BASE_URL=https://cfapi.ltzf.xyz
# 支付结果异步回调地址（必填）。若前后端同站部署在 Vercel，填本站地址即可，例如：
# VITE_LTZF_NOTIFY_URL=https://你的项目.vercel.app/api/pay/ltzf/notify
VITE_LTZF_NOTIFY_URL=https://你的项目.vercel.app/api/pay/ltzf/notify
